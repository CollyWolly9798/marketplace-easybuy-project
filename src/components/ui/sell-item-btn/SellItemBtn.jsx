import style from './SellItemBtn.module.scss';

const SellItemBtn = () => {
  return (
    <button type='button' className={style.btn}>
      Sell Your Item
    </button>
  );
};

export default SellItemBtn;
